const next = require('../src/index');

const app = next();
app.use(function (next, option) {

}).use(function (next, option) {

}).use(function (next, option) {

}).use(function (next, option) {

}).use(function (next, option) {

});

app({
    name: 'xesam',
    age: 18
});